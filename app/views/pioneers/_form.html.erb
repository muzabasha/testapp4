<%= form_with(model: pioneer, local: true) do |form| %>
  <% if pioneer.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(pioneer.errors.count, "error") %> prohibited this pioneer from being saved:</h2>

      <ul>
      <% pioneer.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :first_name %>
    <%= form.text_field :first_name, id: :pioneer_first_name %>
  </div>

  <div class="field">
    <%= form.label :last_name %>
    <%= form.text_field :last_name, id: :pioneer_last_name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
